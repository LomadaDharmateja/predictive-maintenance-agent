# Every entry is either imported by code in this repository or required at
# runtime by something that is; nothing here is aspirational. Regenerate this
# file when imports change -- do not append to it.
#
# Data and feature layers
numpy==2.5.1
# Pinned to the version the suite is actually verified against.
#
# This was `pandas==3.0.5` and that pin never resolved: mlflow 3.15.1 requires
# `pandas<3`, so `pip install -r requirements.txt` failed outright with
# ResolutionImpossible, CI never reached pytest, and the job surfaced as a
# missing report.xml. It went unnoticed locally because this venv was built
# incrementally and has been running 2.3.3 the whole time -- the pinned 3.0.5
# was never installed anywhere. A note in requirements-api.txt claimed "the
# 3.0.5 the feature layer needs"; the feature layer does not need it, and all
# 547 tests pass on 2.3.3.
pandas==2.3.3
pandera==0.32.1

# Parquet engine for pandas.to_parquet / read_parquet in src/features/build.py.
# Not imported by name: pandas declares it only as an optional extra, so it has
# to be pinned here or `make features` fails at the write step.
pyarrow==25.0.0

# Modelling and evaluation
scikit-learn==1.9.0
lightgbm==4.7.0
# Not imported by name anywhere in this repository. Pinned because
# scikit-learn and lightgbm both require it and the determinism rule means the
# numerical stack must be a fixed set rather than whatever a resolver picks on
# the day. `tests/test_requirements.py` carries it as a documented exception.
scipy==1.18.0
joblib==1.5.3

# Experiment tracking. MLflow 3.15 put the filesystem store into maintenance
# mode, so the local backend is SQLite under mlruns/ -- see src/models/train.py.
mlflow==3.15.1

# PR, reliability and cost curves in src/eval/plots.py. Agg backend only; no
# display is required.
matplotlib==3.11.1

# Kaggle download in scripts/fetch_data.py. Imported lazily inside the download
# function so that `--verify` works without it, and so importing the module does
# not emit the library's authentication banner.
kaggle==2.2.4

# Model providers, for recording evaluation transcripts (src/agent/providers.py).
#
# The suite runs offline against recorded transcripts and needs neither of
# these to score a run; they are imported only on the `--live` recording path.
# The Anthropic SDK resolves ANTHROPIC_API_KEY from the environment itself --
# nothing in this repository reads, stores or forwards a key value.
anthropic==0.122.0

# The local Ollama adapter talks to http://127.0.0.1:11434 directly rather than
# through the `ollama` package: one HTTP client for one provider is less
# dependency than a second SDK, and httpx is already resolved as an anthropic
# dependency. Pinned here because src/agent/providers.py imports it by name.
httpx==0.28.1

# Observability. Milestone 6: one span per run, children per tool and model
# call. No exporter here opens a socket -- src/obs/tracing.py ships a JSON-lines
# file exporter and an in-memory one, and the test suite must run with no
# network. An OTLP exporter is deliberately not a dependency.
opentelemetry-sdk==1.44.0

# Service layer. Imported directly by src/api/ and exercised by tests/test_api.py
# and tests/test_demo_ui.py, so the test set needs them and not only the image.
#
# All three previously arrived transitively -- fastapi and pydantic via mlflow
# and pandera, PyYAML via starlette. The suite passed on dependencies nothing
# declared, so dropping mlflow would have broken the API tests for no visible
# reason. `tests/test_requirements.py` now fails the build on any such gap.
fastapi==0.141.1
pydantic==2.13.1

# Scenario definitions in evals/scenarios.yaml, read by evals/runner.py.
PyYAML==6.0.3

# Test
pytest==9.1.1
